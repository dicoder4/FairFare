# --- Basic service info ---
spring.application.name=user-service
server.port=${USER_SERVICE_PORT:3001}

# --- Eureka client config ---
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://localhost:3000/eureka}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# --- MongoDB Atlas config ---
# Added a database name "user-service-db" at the end of the URI
# Default values are provided for development - override with environment variables for production
spring.data.mongodb.uri=mongodb+srv://${MONGODB_USERNAME:1ms22cs051}:${MONGODB_PASSWORD:gyVy64EIEDLCvPrS}@${MONGODB_CLUSTER:cluster0.ak4k3cl.mongodb.net}/user-service-db?retryWrites=true&w=majority
